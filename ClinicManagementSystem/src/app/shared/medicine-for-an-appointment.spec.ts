import { MedicineForAnAppointment } from './medicine-for-an-appointment';

describe('MedicineForAnAppointment', () => {
  it('should create an instance', () => {
    expect(new MedicineForAnAppointment()).toBeTruthy();
  });
});
